#include <iostream>

int main() {
  int x; // delares x exists and is an int
  x = 0; // initializes x to 0
  double y = 0.0; // definition of variable y
  return 0;
}

/**
 * 1. Before a variable can be used, it must be declared.
 * 2. Variables must have some specific type.
 * 3. Variable types never changes.
 * 4. Types are verified before the code runs!
 *    If there are type issues, code will not compile!
 */